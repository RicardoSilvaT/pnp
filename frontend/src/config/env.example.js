// src/config/env.example.js
// Copia este archivo como env.js y configura según tu servidor
const config = {
  // Cambiar según el servidor:
  // Servidor local: 'localhost'
  // Servidor VPS: '161.132.45.223'
  API_HOST: 'localhost'
};

export default config;